import 'package:flutter/material.dart';

class GroomingManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Grooming Management')),
      body: Center(child: Text('Grooming Management Screen')),
    );
  }
}